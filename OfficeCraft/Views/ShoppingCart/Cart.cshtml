@model List<ShoppingCartItem>

<h2>Carrito de Compras</h2>

<ul>
    @foreach (var item in Model)
    {
        <li>
            <img src="@item.Product.ImageUrl" alt="@item.Product.Name" width="50" height="50" />
            @item.Product.Name - Precio: $@item.Product.Price - Cantidad: @item.Quantity

            <form asp-controller="ShoppingCart" asp-action="RemoveFromCart" method="post">
                <input type="hidden" name="productId" value="@item.Product.Id" />
                <button type="submit">Eliminar</button>
            </form>
        </li>
    }
</ul>

<a asp-controller="ShoppingCart" asp-action="Checkout">Hacer Pedido</a>
